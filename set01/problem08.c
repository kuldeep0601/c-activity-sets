#include <stdio.h>

int main()
{
    int n = input_array_size();
    int a[n];
    input_array(n,a);
    int sum = sum_n_array(n,a);
    output(sum);
    return 0;
}
int input_array_size()
{
    int n;
    printf("input array size:");
    for(int i=0;i<n;i++)
    {

    }
}